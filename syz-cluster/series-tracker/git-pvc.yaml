apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: series-tracker-repo-disk-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi
  storageClassName: standard
